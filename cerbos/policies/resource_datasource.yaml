apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: default
  resource: datasource
  rules:
    - actions: ["view"]
      effect: EFFECT_ALLOW
      roles: ["viewer", "admin"]

    - actions: ["edit"]
      effect: EFFECT_ALLOW
      roles: ["admin"]