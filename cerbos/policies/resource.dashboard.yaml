apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: default
  resource: dashboard
  rules:
    - actions: ["view"]
      effect: EFFECT_ALLOW
      roles: ["viewer", "admin"]

    - actions: ["edit", "delete"]
      effect: EFFECT_ALLOW
      roles: ["admin"]
